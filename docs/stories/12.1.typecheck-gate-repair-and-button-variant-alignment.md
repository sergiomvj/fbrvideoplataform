# Story 12.1: Typecheck Gate Repair & Button Variant Alignment

**Epic:** Final Compliance Hardening  
**Story ID:** 12.1  
**Sprint:** Final QA Closure  
**Priority:** Critical  
**Points:** 3  
**Effort:** 2-4 hours  
**Status:** Approved  
**Type:** Frontend

---

## Why This Exists

The project is not in final compliance while the root `typecheck` gate fails. The current failure is concrete and reproducible:

- `npm run typecheck`
- error in [review-actions.tsx](C:/Projetos/1FBRFaceVideo/frontend/components/review-queue/review-actions.tsx:19)

The failure blocks the validity of Story `11.6` and the repository quality gates.

---

## Story

**As a** QA closure owner,  
**I want** the root typecheck gate to pass cleanly,  
**so that** the project can be considered technically compliant with the declared quality gates.

---

## Acceptance Criteria

1. `npm run typecheck` passes from the repository root.
2. `frontend/components/review-queue/review-actions.tsx` uses only button variants that are actually supported by the design system.
3. If `success` is intended as a canonical button variant, the `Button` component type and styling support it explicitly and consistently.
4. No local type workaround or unsafe cast is introduced just to silence the error.

---

## Detailed Implementation Instructions

### Step 1: Inspect the Button contract

Open:

- [button.tsx](C:/Projetos/1FBRFaceVideo/frontend/components/ui/button.tsx)
- [review-actions.tsx](C:/Projetos/1FBRFaceVideo/frontend/components/review-queue/review-actions.tsx)

Confirm exactly which variants are supported by `ButtonVariant`.

### Step 2: Choose one valid correction path

Use **only one** of these paths:

#### Path A: `success` is not part of the design system

Then:

- replace `variant="success"` in [review-actions.tsx](C:/Projetos/1FBRFaceVideo/frontend/components/review-queue/review-actions.tsx:19)
- use an already supported semantic variant that matches the intended UX
- preserve visual distinction between approve/reject/requery actions

Recommended mapping:

- approve -> existing positive/primary-safe variant
- reject -> existing danger variant
- requery -> existing secondary/neutral variant

#### Path B: `success` is supposed to be official

Then:

- add `success` to the `ButtonVariant` type in [button.tsx](C:/Projetos/1FBRFaceVideo/frontend/components/ui/button.tsx)
- implement styling for `success`
- ensure the variant is not just typed, but visually defined
- verify no other component logic assumes a closed set that would now break

### Step 3: Re-run the gate

From repo root:

```powershell
npm run typecheck
```

The story is **not done** unless it passes.

---

## Forbidden Fixes

- Do not use `as any`
- Do not use `as ButtonVariant` to force the type
- Do not comment out the offending prop
- Do not remove the approve action styling distinction unless the design system explicitly requires it

---

## Relevant Files

```text
frontend/components/review-queue/review-actions.tsx
frontend/components/ui/button.tsx
package.json
```

---

## Definition of Done

- code updated
- `npm run typecheck` passes from root
- no unsafe type suppression introduced
- story file updated by dev with real evidence

---

## QA Results

Pending
